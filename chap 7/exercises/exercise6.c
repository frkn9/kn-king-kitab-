#include <stdio.h>

int main() {

    // Days in a month(30): char
    // Days in a year(365): short
    // minutes in a day(1500): short
    // seconds in a day(86870): long
    /*As char 's size is always the minimum supported data type, no other data types (except bit-fields) can be smaller.
    The minimum size for char is 8 bits, the minimum size for short and int is 16 bits, for long it is 32 bits and 
    long long must contain at least 64 bits.*/
    /*The minimum size for char is 8 bits, the minimum size for short and int is 16 bits, for long it is 32 bits and 
    long long must contain at least 64 bits.*/

}